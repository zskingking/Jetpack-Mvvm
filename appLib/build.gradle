apply plugin: 'com.android.library'
apply from: "../basic.gradle"

android{
    dataBinding {
        enabled = true
    }
}
dependencies {
    api project(':zsLib')
    api(ThirdParty.blockCanary.blockCanary)

}